create or replace package body k_mapping is

	procedure set is
		-- v_sects st;
	begin
		null;
		/*
    t.split(v_sects, r.pathname, '/');
    r.setc('x$dbu', v_sects(2));
    r.setc('x$prog', v_sects(3);
    */
	end;

end k_mapping;
/
