create or replace context gac_speed_test using dbms_session accessed globally;
create or replace context key_ver_ctx using kv accessed globally;
create or replace context server_process using dbms_session accessed globally;
