create or replace context gac_speed_test using sys.dbms_session accessed globally;
create or replace context key_ver_ctx using kv accessed globally;
create or replace context server_process using sys.dbms_session accessed globally;
