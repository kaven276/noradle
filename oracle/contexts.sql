create or replace context sess_cid_ctx using k_gc accessed globally;
create or replace context key_ver_ctx using kv accessed globally;
-- create or replace context ua_ctx using r accessed globally;
